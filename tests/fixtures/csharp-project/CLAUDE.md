# My C# Project

- Enable nullable reference types to avoid null reference exceptions.
- Run dotnet format before every commit.
- Use xUnit for all unit tests.
- Use PascalCase for all public members and types.
- Use global usings to reduce boilerplate.
- Remove unused variables and dead code (IDE0059).
